# Captura del stream
yt-dlp
opencv-python-headless

# Detección y tracking
ultralytics
supervision

# LLM verificador (compatible con HuggingFace y Ollama)
httpx

# Panel web
dash
plotly
waitress

# Notificaciones
python-telegram-bot

# TTS — Kokoro (requiere: apt install espeak-ng)
kokoro>=0.9.2
sounddevice
soundfile

# Utilidades
pyyaml
python-dotenv
loguru
numpy
